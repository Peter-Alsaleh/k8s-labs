apiVersion: v1
kind: Service
metadata:
  labels:
    app: flask-app
  name: service-flask
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: flask-app
  type: LoadBalancer
status:
  loadBalancer: {}
